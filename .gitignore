# Binaries. Anchor these to the repo root: an unanchored "wavonyx" would also
# match the cmd/wavonyx source directory and quietly keep the CLI out of git.
bin/
/wavonyx
/wavonyx-*
*.exe

# Go
*.test
*.out
coverage.txt
coverage.html

# Data (SQLite session store)
data/
*.db
*.db-wal
*.db-shm

# Env
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Build artefacts
dist/
out/
